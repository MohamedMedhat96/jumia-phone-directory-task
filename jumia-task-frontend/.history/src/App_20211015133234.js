import { Table } from "antd";

function App() {
  const stateSearch = () => {
    return <div>
      <search></search> </div>
  }
  const columns = [{ title: "State", dataIndex: "state", key: "state", filterDropdown: }]
  return (
    <Table columns>

    </Table>
  );
}

export default App;
